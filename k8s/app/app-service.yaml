apiVersion: v1
kind: Service
metadata:
  name: laravel-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: laravel-app
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
  - name: websocket
    port: 8080
    targetPort: 8080
    nodePort: 30088
